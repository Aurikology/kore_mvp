<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- API 33+ only. Below it the OS grants posting by default, which is why
         the host answers `requestNotificationPermission` with
         `areNotificationsEnabled()` there rather than with a request. Declared
         even so: the permission is harmless on older releases and its absence
         on 33+ makes the request silently deny. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- API 31+, both asked for at runtime together. `neverForLocation` is an
         assertion rather than a hint: it says KORE never derives the user's
         position from a scan result, which is true - the only thing
         `KoreBleHost` does with one is connect to it - and it is what lets the
         scan run on 31+ without a location permission at all. Without the flag
         the platform demands location as well, and a user declining that reads
         as a patch that simply never appears. -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s"/>
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"/>

    <!-- Below 31, where `minSdk` 24 means half the ways this app can be
         installed. BLUETOOTH and BLUETOOTH_ADMIN are install-time and granted
         by their presence here; ACCESS_FINE_LOCATION is the one the user has to
         be asked for, because a pre-31 BLE scan is legally a location
         capability - and coarse is not enough, since a coarse-only grant on
         29-30 yields a scan that returns nothing with no error at all. Capped
         at 30 so a modern install does not carry a location permission it can
         never use. -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"
        android:maxSdkVersion="30"/>

    <!-- Not required: a phone with no LE radio still installs and still runs,
         because `createEegSource()` falls back to the simulated source. Marking
         it required would hide the app on Play from devices that can run all of
         it except the patch. -->
    <uses-feature android:name="android.hardware.bluetooth_le"
        android:required="false"/>
    <application
        android:label="KORE"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- `Not now`. A receiver rather than an activity because the whole
             point of that button is that it does not raise the app; not
             exported, so the only thing that can fire it is the PendingIntent
             KORE itself attached to the banner. -->
        <receiver
            android:name=".NotificationActionReceiver"
            android:exported="false"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
